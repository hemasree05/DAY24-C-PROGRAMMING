#include<stdio.h>
main()
{
	int n,r,sum,num=0;
	printf("enter a number");
	scanf("%d",&n);
	num=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(sum==num)
	printf("armstrong");
	else
	printf("not an armstrong");
}
